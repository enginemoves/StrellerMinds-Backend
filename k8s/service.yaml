apiVersion: v1
kind: Service
metadata:
  name: strellerminds-backend-service
  namespace: strellerminds-staging
  labels:
    app: strellerminds-backend
    environment: staging
spec:
  selector:
    app: strellerminds-backend
    color: blue
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: LoadBalancer
---
apiVersion: v1
kind: Service
metadata:
  name: strellerminds-backend-service-blue
  namespace: strellerminds-staging
  labels:
    app: strellerminds-backend
    color: blue
    environment: staging
spec:
  selector:
    app: strellerminds-backend
    color: blue
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: strellerminds-backend-service-green
  namespace: strellerminds-staging
  labels:
    app: strellerminds-backend
    color: green
    environment: staging
spec:
  selector:
    app: strellerminds-backend
    color: green
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: strellerminds-backend-service
  namespace: strellerminds-production
  labels:
    app: strellerminds-backend
    environment: production
spec:
  selector:
    app: strellerminds-backend
    color: blue
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: LoadBalancer
---
apiVersion: v1
kind: Service
metadata:
  name: strellerminds-backend-service-blue
  namespace: strellerminds-production
  labels:
    app: strellerminds-backend
    color: blue
    environment: production
spec:
  selector:
    app: strellerminds-backend
    color: blue
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: strellerminds-backend-service-green
  namespace: strellerminds-production
  labels:
    app: strellerminds-backend
    color: green
    environment: production
spec:
  selector:
    app: strellerminds-backend
    color: green
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  type: ClusterIP
